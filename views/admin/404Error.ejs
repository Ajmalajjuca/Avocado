<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 - Page Not Found</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap');

        body {
            font-family: 'Roboto', sans-serif;
            background-color: #f6f6f6;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            text-align: center;
            max-width: 600px;
            padding: 20px;
        }
        h1 {
            font-size: 3rem;
            color: #5c6a7c;
            margin-bottom: 10px;
            animation: fadeInDown 1s ease-out;
        }
        p {
            font-size: 1.2rem;
            color: #7b8794;
            margin-bottom: 30px;
            animation: fadeIn 1s ease-out 0.5s both;
        }
        .emoji {
            font-size: 1.5rem;
            animation: shake 0.5s ease-in-out infinite;
            display: inline-block;
        }
        .btn {
            background-color: #088178;
            color: white;
            padding: 12px 24px;
            border-radius: 30px;
            text-decoration: none;
            font-weight: bold;
            transition: all 0.3s ease;
            animation: fadeIn 1s ease-out 1s both;
        }
        .btn:hover {
            background-color: #076f68;
            transform: translateY(-3px);
            box-shadow: 0 4px 10px rgba(0,0,0,0.2);
        }
        .image-container {
            margin-top: 40px;
            animation: fadeInUp 1s ease-out 1.5s both;
        }
        img {
            max-width: 100%;
            height: auto;
        }
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        @keyframes fadeInDown {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        @keyframes shake {
            0% { transform: rotate(0deg); }
            25% { transform: rotate(20deg); }
            75% { transform: rotate(-20deg); }
            100% { transform: rotate(0deg); }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Page Not Found :(</h1>
        <p>Oops! <span class="emoji">😖</span> The requested URL was not found on this server.</p>
        <a href="/admin" class="btn">Back to home</a>
        <div class="image-container">
            <img src="/user/img/page-misc-error-light.png" alt="404 Error Illustration">
            
        </div>
    </div>
</body>
</html>